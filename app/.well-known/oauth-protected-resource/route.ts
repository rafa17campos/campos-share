import { handleProtectedResourceMetadata } from '../../../lib/oauth/handlers.ts';

export const dynamic = 'force-dynamic';

export { handleProtectedResourceMetadata as GET, handleProtectedResourceMetadata as OPTIONS };
